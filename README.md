# NuScope
MCP server for exploring NuGet packages, metadata, and source code.

## Development

```powershell
dotnet restore
dotnet build
dotnet test
```

Run the skeleton MCP server over stdio:

```powershell
dotnet run --project src/NuScope.Server/NuScope.Server.csproj
```
